# TODO

## Plugin Polish

- [ ] Add `README.md` for each plugin under `plugins/`.
- [ ] Rewrite plugin `shortDescription` fields as complete short sentences.
- [ ] Keep root `README.md` structure examples aligned with real directories.
- [ ] Stop `scripts/new-plugin.sh` from creating empty optional folders by default.
- [ ] Extend `scripts/validate.sh` to check command-to-agent references if commands are added.

## Release

- [ ] Reinstall changed plugins in Codex after pulling the Git marketplace.
- [ ] Start a new Codex thread after reinstalling so new skills load.
